apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantis-redis
  labels:
    app: quantis-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantis-redis
  template:
    metadata:
      labels:
        app: quantis-redis
    spec:
      containers:
        - name: quantis-redis
          image: redis:6.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6379
          resources:
            limits:
              cpu: 300m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: quantis-redis-data
              mountPath: /data
      volumes:
        - name: quantis-redis-data
          persistentVolumeClaim:
            claimName: quantis-redis-pvc
